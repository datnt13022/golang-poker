package player

// Player represents a player in the poker game
type Player struct {
	// Add fields for player details here
	// For example: name, chips, current bet, etc.
}

// NewPlayer creates a new instance of the Player
func NewPlayer() *Player {
	return &Player{
		// Initialize player details here
	}
}
